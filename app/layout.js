export const metadata = {
  title: "Buy vs Rent — India",
  description: "An interactive India-focused buy vs rent calculator.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
